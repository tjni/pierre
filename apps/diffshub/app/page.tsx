// The diffshub home page. The implementation lives in `_home/Home.tsx` so it
// keeps its sibling imports; this route file just re-exports it as the `/`
// default.
export { default } from './_home/Home';
