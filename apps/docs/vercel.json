{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "set -e; export PATH=\"$PWD/node_modules/.bin:$PWD/apps/docs/node_modules/.bin:$PWD/../../node_modules/.bin:$PATH\"; moon run docs:build-deps; if [ -d apps/docs ]; then cd apps/docs; fi; bun scripts/generate-llms-txt.ts; next build"
}
